@model IEnumerable<Zika.Models.Pricing>

@{
    Layout = "_AdminLayout";
    ViewData["Title"] = "Index";
}

<h2>Index</h2>

<p>
    <a asp-action="Create">Create New</a>
</p>
<table class="table table-bordred table-striped">
    <thead>
        <tr>
                
                <th>
                    @Html.DisplayNameFor(model => model.PricingInformation)
                </th>
            <th></th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            
            <td>
                @Html.DisplayFor(modelItem => item.PricingInformation)
            </td>
            <td>
                @Html.ActionLink("Edit", "Edit", new { id = item.PricingId }) |
                @Html.ActionLink("Delete", "Delete", new { id = item.PricingId })
            </td>
        </tr>
}
    </tbody>
</table>
